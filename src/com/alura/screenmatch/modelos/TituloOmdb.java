package com.alura.screenmatch.modelos;
//dentro de los parentecisis se mete los atributos que se quieren que se muestren en el json
public record TituloOmdb(String title, String year, String runtime) {
}
